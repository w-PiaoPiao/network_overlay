# NetworkOverlay 打包说明

> **⭐ 重要原则：只有用户明确要求打包时，才执行下面的打包流程。**
> 日常改动（修 bug、改功能）只需直接修改 `network_overlay.pyw` ——
> 因为开机自启走的是注册表 → `pythonw + network_overlay.pyw`，**不经过 exe 打包**。

## 当前版本
- v1.2.1（写于 `version_info.txt`）
- 源文件 `network_overlay.pyw` 已包含 v1.2.1 的全部修复，直接可用。

## v1.2.1 变更摘要
- **修复**：锁定模式下悬浮窗渲染成纯黑色块、完全无文字。
  - 根因：`_set_click_through(True)` 在 tkinter 已经通过 `-alpha` 把窗口设为分层窗口之后，
    又用 `SetWindowLongPtr` 重复断言 `WS_EX_LAYERED`，破坏了分层重定向表面 → 窗口整体渲染成黑块。
  - 修复：锁定/解锁只切换 `WS_EX_TRANSPARENT`（鼠标穿透），不再触碰 `WS_EX_LAYERED`。
  - 影响：开机自启路径（pythonw + .pyw）立即生效，**无需打包**。
- **新增**：托盘图标右键弹出专用菜单，顶部「❌ 退出 NetworkOverlay」一键关闭；
  悬浮窗异常（如黑块）或锁定穿透时，托盘是唯一可靠关闭入口。

## ⚠️ 已知问题：单文件打包（onefile）在本机无法启动
- 现象：`dist\NetworkOverlay.exe` 一启动就弹 `Error: Failed to extract entry: VCRUNTIME140.dll`。
- 原因：PyInstaller 单文件版启动时要把 `VCRUNTIME140.dll` 等运行库解压到 `%TEMP%\_MEIxxxxx`；
  本机安全策略/杀软拦截了临时目录解压/执行，导致失败。**与代码无关，重试无效。**
- 处置：改用**目录版（onedir）**打包，运行库随目录一起发布，无需临时解压。

## 打包命令
（仅当用户明确要求打包时执行；在项目根目录运行）

### 方式一：目录版（onedir）——推荐，本机可用
```bat
"C:\Users\Jim Wang\AppData\Local\Programs\Python\Python312\python.exe" -m PyInstaller --noconfirm --clean --onedir --windowed --name NetworkOverlay --version-file version_info.txt network_overlay.pyw
```
产物：`dist\NetworkOverlay\`（含 `NetworkOverlay.exe` + `_internal\` 运行库）。
发布时把整个 `NetworkOverlay\` 目录 + `README.txt` 一起归档。

### 方式二：单文件版（onefile）——其他机器可能可用，本机不可用
```bat
"C:\Users\Jim Wang\AppData\Local\Programs\Python\Python312\python.exe" -m PyInstaller --noconfirm --clean --onefile --windowed --name NetworkOverlay --version-file version_info.txt network_overlay.pyw
```
产物：`dist\NetworkOverlay.exe`。

### 关于仓库里的 `NetworkOverlay.spec`
- 该 spec 目前是**单文件版**（`EXE(...)`，无 `COLLECT`）。
- 在本机产出的单文件 exe **无法启动**（见上面的 VCRUNTIME140.dll 问题）。
- 若要用命令行之外的 spec 方式打成可用版本，需先改成 onedir（`EXE(...)` + `COLLECT(...)`）。

## 发布归档参考
- 既有归档格式：`NetworkOverlay-v1.2.0.zip` = `NetworkOverlay.exe` + `README.txt`（单文件）。
- onedir 版归档：应包含 `NetworkOverlay\` 整个目录（exe + `_internal\`）+ `README.txt`。

## 其他备注
- `dist\NetworkOverlay.exe`（当前遗留）是之前一次打包尝试的产物，**在本机不可运行**，勿直接分发。
- 如需重新打包，请先明确对用户确认：`打包成单文件版还是目录版？目标机器是哪台？`
